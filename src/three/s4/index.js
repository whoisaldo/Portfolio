// src/three/s4/index.js: the car the garage orbits.
//
// The S4 itself is object.js beside this file, built by GPT-6-Astra through
// the codex-3d skill from the three photographs in src/assets/Photos (see
// notes.md there for the part names). This module is the one import the
// scene makes, so the model can be replaced without touching the scene.
export { createObject, meta } from "./object.js";
